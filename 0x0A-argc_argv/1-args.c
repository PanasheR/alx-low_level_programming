#include <stdlib.h>
#include <stdio.h>

/**
 * main - returns number of strings
 * @argc: count
 * @argv: string array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv)
{
	printf("%d", argc);
	printf("\n");
	return (0);
}
